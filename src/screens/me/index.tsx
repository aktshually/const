import { Text, View } from "styles";

export const Me = () => {
	return (
		<View class="flex items-center justify-center h-screen">
			<Text class="font-poppins">Hello world from me</Text>
		</View>
	);
};
