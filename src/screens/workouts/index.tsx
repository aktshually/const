import { Text, View } from "styles";

export const Workouts = () => {
	return (
		<View class="flex items-center justify-center w-screen h-screen">
			<Text class="font-urbanist">Hello world from me</Text>
		</View>
	);
};
